export type Project = {
    id: string;
    title: string;
    shortDescription: string;
    fullDescription: string;
    image: string;
    repo?: string;
};

export const projects: Project[] = [
    
]